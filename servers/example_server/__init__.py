"""Example MCP server wrapper.

Replace this with a real MCP server. Each tool is a thin Python wrapper
that calls the underlying MCP tool via the sandbox bridge.

Available tools:
    - ping_device: Check if an AV device is reachable
    - get_device_info: Retrieve device details via REST API
"""
